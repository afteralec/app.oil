version: "2"
sql:
  - engine: "mysql"
    queries: "queries"
    schema: "../db/migrations"
    gen:
      go:
        package: "queries"
        out: "internal/queries"
        emit_prepared_queries: true
